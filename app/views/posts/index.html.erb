<% @posts.each do |post| %>
  <div class="post_wrapper">
    <h2 class="title"><%= link_to post.title, post %></h2>
    <p class="body"><%= truncate(post.body, length: 200) %></p>
    <p class="date">About <%= time_ago_in_words(post.created_at) %></p>
  </div>
<% end %>